# Ubuntu specific variables
package_manager: apt
package_update_cmd: "apt update"
package_install_cmd: "apt install -y"

distro_packages:
  - build-essential
  - software-properties-common
  - apt-transport-https
  - ca-certificates
  - gnupg
  - lsb-release
  - python3-pip
  - qemu-system
  - ubuntu-restricted-extras  # Ubuntu-specific
  - fonts-firacode
  - openssh-client
  - network-manager
  - python3-psutil
