# Debian specific variables
package_manager: apt
package_update_cmd: "apt update"
package_install_cmd: "apt install -y"

distro_packages:
  - build-essential
  - apt-transport-https
  - ca-certificates
  - gnupg
  - lsb-release
  - python3-pip
  - qemu-system
  - firmware-linux-nonfree  # Debian-specific
